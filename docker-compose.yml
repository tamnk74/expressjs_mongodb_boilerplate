version: '3'
services:
  node-app:
    build: .
    image: node-app
    command: npm start
    # env_file: .env
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/node-boilerplate
      # - MONGO_USERNAME=$MONGO_USERNAME
      # - MONGO_PASSWORD=$MONGO_PASSWORD
      # - MONGO_HOSTNAME=db
      # - MONGO_PORT=$MONGO_PORT
      # - MONGO_DB=$MONGO_DB
    ports:
      - '3000:3000'
    depends_on:
      - mongodb
    volumes:
      - .:/var/www
    networks:
      - node-network
  mongodb:
    image: mongo
    container_name: mongodb
    hostname: mongo
    ports:
      - 27017:27017
    restart: always
    networks:
      - node-network
  mongo-express:
    image: mongo-express
    container_name: mongo_exporess
    restart: always
    links:
      - mongodb
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_SERVER: mongodb
      ME_CONFIG_MONGODB_PORT: 27017
    networks:
      - node-network
  redis:
    image: redis:alpine
    container_name: redis
    privileged: true
    ports:
      - 6379:6379
    restart: always
    networks:
      - node-network

volumes:
  dbdata:
  node_modules:

networks:
  node-network:
    driver: bridge
